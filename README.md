# AI Startup Global

Static website preview for AI Startup Global.

## Local preview

```powershell
node server.js
```

Then open `http://localhost:5500`.

## Production readiness

The current account, membership, registration, and admin data use browser-local storage. Connect a shared backend such as Supabase before accepting real user registrations.
