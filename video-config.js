/* Add the ShouldWeTV endpoint when it is ready. Expected response: an array,
   or { videos: [...] }. Video fields: id, title, titleZh, description,
   descriptionZh, category, categoryZh, thumbnail, videoUrl, duration. */
window.SHOULDWE_TV = {
  apiUrl: "",
  apiKey: "",
  limit: 6
};
