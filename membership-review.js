let reviewProfile=null,pendingRequest=null;
const reviewLanguage=()=>document.documentElement.lang==='zh-CN';
async function loadReviewState(){try{reviewProfile=await window.AISGData.profile();if(reviewProfile){const{data}=await window.supabaseClient.from('membership_requests').select('*').eq('user_id',reviewProfile.id).eq('status','pending').maybeSingle();pendingRequest=data||null}}catch(error){console.warn('Membership review:',error.message)}renderUpgradeReview()}
function renderUpgradeReview(){const button=document.querySelector('[data-upgrade]'),card=document.querySelector('[data-paid-card]');if(!button||!reviewProfile)return;let note=card.querySelector('[data-upgrade-rule]');if(!note){note=document.createElement('p');note.className='upgrade-rule';note.dataset.upgradeRule='';button.before(note)}const attended=Number(reviewProfile.attended_events||0),paid=reviewProfile.membership_level==='paid',zh=reviewLanguage();button.classList.remove('eligibility-locked');if(paid){note.textContent=zh?'你的付费会员资格已生效。':'Your paid membership is active.';button.disabled=true;return}if(attended<1){note.textContent=zh?'升级条件：需要由管理员确认至少参加过 1 场线上或线下活动。':'Eligibility: an admin must verify attendance at least one online or in-person event.';button.textContent=zh?'申请升级 →':'Request upgrade →';button.disabled=false;button.classList.add('eligibility-locked');button.onclick=showEligibilityNotice;return}if(pendingRequest){note.textContent=zh?'申请已提交，正在等待管理员审核。':'Application submitted and awaiting admin review.';button.textContent=zh?'审核中':'Under review';button.disabled=true;return}note.textContent=zh?'你已满足活动参与条件，可以提交付费会员申请。':'You meet the attendance requirement and can apply for paid membership.';button.textContent=zh?'提交升级申请 →':'Submit upgrade request →';button.disabled=false;button.onclick=submitMembershipRequest}
function showEligibilityNotice(){let modal=document.querySelector('[data-eligibility-modal]');if(!modal){modal=document.createElement('div');modal.className='eligibility-modal';modal.dataset.eligibilityModal='';modal.innerHTML='<div class="auth-backdrop" data-eligibility-close></div><section class="eligibility-dialog" role="dialog" aria-modal="true"><button class="auth-close" type="button" data-eligibility-close>×</button><span class="eligibility-icon">01</span><p data-eligibility-kicker></p><h2 data-eligibility-title></h2><p data-eligibility-copy></p><div><a class="btn dark" href="apply.html" data-eligibility-events></a><button class="btn ghost" type="button" data-eligibility-close data-eligibility-later></button></div></section>';document.body.append(modal);modal.querySelectorAll('[data-eligibility-close]').forEach(item=>item.addEventListener('click',closeEligibilityNotice))}const zh=reviewLanguage();modal.querySelector('[data-eligibility-kicker]').textContent=zh?'会员升级条件':'MEMBERSHIP REQUIREMENT';modal.querySelector('[data-eligibility-title]').textContent=zh?'需要先参加一次活动':'Join one event first';modal.querySelector('[data-eligibility-copy]').textContent=zh?'参加至少一次线上或线下活动，并由管理员确认后，才可以申请升级为付费会员。':'Attend at least one online or in-person event and have it verified by an admin before applying for paid membership.';modal.querySelector('[data-eligibility-events]').textContent=zh?'查看可参加的活动 →':'Explore events →';modal.querySelector('[data-eligibility-later]').textContent=zh?'稍后再说':'Maybe later';modal.classList.add('open');document.body.classList.add('modal-open')}
function closeEligibilityNotice(){document.querySelector('[data-eligibility-modal]')?.classList.remove('open');document.body.classList.remove('modal-open')}
async function submitMembershipRequest(){if(!reviewProfile||Number(reviewProfile.attended_events||0)<1){showEligibilityNotice();return}const button=document.querySelector('[data-upgrade]');button.disabled=true;try{pendingRequest=await window.AISGData.requestMembership();const endpoint=window.NOTIFICATION_CONFIG?.membershipReviewEndpoint;if(endpoint)await fetch(endpoint,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({id:pendingRequest.id,userId:reviewProfile.id,email:reviewProfile.email,name:reviewProfile.full_name,attendedEvents:reviewProfile.attended_events,status:'pending',requestedAt:pendingRequest.requested_at,adminEmail:window.NOTIFICATION_CONFIG.adminEmail,siteUrl:location.origin+'/admin.html'})})}catch(error){alert(error.message)}renderUpgradeReview()}
document.querySelector('[data-portal-lang]')?.addEventListener('click',()=>setTimeout(renderUpgradeReview,0));loadReviewState();
